language: node_js
node_js: '8.9.4'
addons:
  firefox: 'latest'
dist: trusty
sudo: false

before_script:
  - 'export DISPLAY=:99.0'
  - 'sh -e /etc/init.d/xvfb start'
  - sleep 3 # give xvfb some time to start

before_install: npm install karma-cli -g
