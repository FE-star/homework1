language: node_js
node_js:
  - "10"

addons:
  chrome: stable
services: 
  - xvfb

before_script:
  - "export DISPLAY=:99.0"
  - "sh -e /etc/init.d"
  - sleep 3 # give xvfb some time to start

before_install:
  npm install karma-cli -g