language: node_js
node_js:
  - "6"

# Travis-CI Caching
cache:
  directories:
    - node_modules

addons:
  chrome: stable


dist: xenial
services:
  - xvfb

script: xvfb-run make test

before_install:
  npm install karma-cli -g