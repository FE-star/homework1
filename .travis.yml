language: node_js
node_js:
  - "6"

addons:
  chrome: stable

services:
  - xvfb

script: karma start karma.conf.js --single-run

before_script:
  - "export DISPLAY=:99.0"

before_install:
  npm install karma-cli -g